# Vehicle Sound ML Project

This project classifies vehicle type and estimates speed based on engine audio.

## Prerequisites
Always run the project in venv.

## Structure
- `data/` : audio and features
- `src/` : source code
- `scripts/` : preprocessing and feature generation
- `models/` : saved models
- `notebooks/` : experiments
